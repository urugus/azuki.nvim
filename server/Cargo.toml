[package]
name = "azuki-server"
version = "0.1.0"
edition = "2021"
description = "Japanese input conversion server for azuki.nvim"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
byteorder = "1.5"

[profile.release]
lto = true
strip = true
